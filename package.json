{
  "name": "koalalander",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "deploy": "firebase deploy --only hosting",
    "functions:build": "cd functions && npm run build && cd ..",
    "functions": "firebase deploy --only functions",
    "emulate": "npm run functions:build && firebase emulators:start --only functions",
    "all": "npm run generate && npm run deploy"
  },
  "devDependencies": {
    "@tailwindcss/typography": "^0.5.10",
    "autoprefixer": "^10.4.16",
    "nuxt": "^3.9.0",
    "nuxt-icon": "^0.6.8",
    "postcss": "^8.4.32",
    "tailwindcss": "^3.4.0",
    "vue": "^3.4.3",
    "vue-router": "^4.2.5"
  },
  "dependencies": {
    "firebase-admin": "^12.5.0",
    "firebase-functions": "^6.0.0"
  }
}
